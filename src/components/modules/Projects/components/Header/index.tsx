import React from "react";

type Props = {};

const Header = (props: Props) => {
  return (
    <section className="space-y-6 mt-3 p-4 lg:p-8 rounded-md border bg-card text-card-foreground">
      <div className="space-y-2">kdefw</div>
    </section>
  );
};

export default Header;
