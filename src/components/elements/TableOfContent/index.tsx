import React from "react";

type TableOfContentProps = {
  element: any;
};

const TableOfContent = ({ element }: TableOfContentProps) => {
  return <div>{element}</div>;
};

export default TableOfContent;
